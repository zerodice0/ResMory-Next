import { style } from "@vanilla-extract/css";

export const textReviewContainer = style({
  display: "flex",
});

export const textReview = style({
  height: "10rem",
  borderRadius: "0.5rem",
  margin: "1rem",
  display: "flex",
  flexGrow: 1,
  border: "1px solid #00000050",
});